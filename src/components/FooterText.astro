---
export interface Props {
  body?: string;
  icon?: string;
}

const {
  body = '289 Borno Way Alagomeji-Yaba Lagos, Nigeria.',
  icon = 'location_on',
} = Astro.props;
---

<div class="info">
  <span class="material-icons">{icon}</span>
  <p>{body}</p>
</div>

<style>
  .info {
    display: flex;
    flex-direction: row;
    justify-content: space-between;
    align-items: flex-start;
    flex-wrap: wrap;
    padding: 10px 25px;
  }
  h2 {
    margin-left: 25px;
  }
</style>
